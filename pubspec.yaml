name: bracket_view
description: A reusable tournament bracket widget with scroll-driven animations and responsive layout.
version: 0.2.0
repository: https://github.com/oohyugi/bracket-view

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
